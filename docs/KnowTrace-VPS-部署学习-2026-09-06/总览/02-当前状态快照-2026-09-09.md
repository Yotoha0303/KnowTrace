# VPS 当前状态快照（2026-09-09）

核验方式：从 Windows 使用 `ssh knowtrace-vps` 对 VPS 执行只读命令；未重启、未修改服务。

## 系统与版本

| 项目 | 核验结果 |
| --- | --- |
| 操作系统 | Ubuntu 24.04.4 LTS |
| 内核 | 6.8.0-139-generic |
| Docker | 29.1.3 |
| Docker Compose | 2.40.3 |
| Nginx | 1.24.0 |
| Caddy | 2.11.4 |
| Git | 2.43.0 |
| Python | 3.12.3 |
| 项目分支 | `codex/stage3-observability` |
| VPS 仓库 HEAD | `31f49641d310c693d829ce8ca6b7a24078a196ed` |

VPS HEAD 与 `origin/codex/stage3-observability` 一致；`compose.production.yaml` 是服务器专用未跟踪 overlay。

## 入口与安全边界

- DNS：`knowtrace.duckdns.org → 45.64.74.99`；
- 首页：HTTP 307 跳转 `/login`，登录页 HTTP 200；
- 正确健康端点：应用与边缘链路 `/api/health/live`、`/api/health/ready` 均为 200；
- 公网 `/api/metrics` 返回 404；
- TLS 证书域名匹配，Let's Encrypt 签发，有效期至 2026-12-05；
- 公网监听：80、443、SSH 22345；
- Grafana 3001、Prometheus 9090、Alertmanager 9093、Elasticsearch 9200、Kibana 5601 均只绑定 `127.0.0.1`；
- UFW active，当前允许 Nginx Full 与 22345/tcp，没有恢复 22/tcp 放行。

注意：`/api/ready` 不是项目健康端点，它返回 401 不能作为故障证据；必须使用 `/api/health/ready`。

## 应用与可观测性

- KnowTrace app、auth、PostgreSQL、MySQL、Redis 均为 running/healthy；
- Prometheus、Grafana、Alertmanager 均为 running/healthy；
- Prometheus 12/12 active targets UP；
- Prometheus 与 Alertmanager 当前均无 active alert；
- Elasticsearch 8.19.21、Logstash 8.19.21、Kibana 8.19.21 当前已启动且 healthy；
- Grafana 12.2.0，Prometheus 3.5.5，Alertmanager 0.28.1；
- 邮件告警状态仍为 `local-only`，没有真实收件证据。

## 资源快照

ELK 启动后的只读快照：

- 物理内存约 1.8 GiB，可用约 303 MiB；
- swap 总量 4 GiB，已使用约 2 GiB；
- 根文件系统 57 GiB，已使用约 23 GiB，可用约 32 GiB；
- Elasticsearch 约 329 MiB、Kibana约 292 MiB、Logstash约 316 MiB。

结论：ELK 可以在该机器上按需运行，但已经产生明显 swap 压力，不适合作为长期常驻、可承诺性能的生产日志平台。

## 备份

- `knowtrace-backup.timer` 为 active/enabled；
- 2026-09-09 核验时下一次调度仍存在；
- VPS 中至少保留了 2026-09-07 至 2026-09-08 的多个备份归档；
- 本地学习包没有复制明文归档、数据库 dump、Redis RDB 或真实附件。

## 证据限制

本快照证明检查时刻的状态，不代表持续可用。运行中的 app 报告应用 revision 与仓库当前运维 HEAD 不同，原因可能是后续提交只更新运维文件而没有重建 app；在生产化前应生成统一、不可变的 release manifest 明确代码、镜像和配置版本。

## 静态配置复核

本次只读复核结果：

- 所有保存的 Bash 运维脚本通过 `bash -n`；
- `load-baseline.py` 与 `verify-observability.py` 通过 Python AST 解析；
- 合并后的 Docker Compose 配置通过 `config --quiet`；
- Nginx 配置通过 `nginx -t`；
- SSH 配置通过 `sshd -t`；
- Caddyfile 通过 `caddy validate`；
- 本地 40 份服务器文件副本与 VPS 源文件 SHA-256 全部一致；
- 本地 38 份 Markdown 中 5 个 Mermaid 代码块围栏完整，内部相对链接无缺失。

未安装 Mermaid CLI，因此 Mermaid 图只完成了文本结构检查，尚未通过独立渲染器做视觉验收。
