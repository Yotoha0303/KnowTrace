# 2026-09-08 ELK 端到端与资源证据

## 模式

VPS 只有约 1.8 GiB RAM。Elasticsearch、Logstash、Kibana 使用 Compose `elk` profile 按需启动，完成验证后停止，数据卷保留。

## 最终端到端验收

```text
PASS Elasticsearch: HTTP 200
PASS Kibana: HTTP 200
PASS Logstash TCP input accepted verification event: stage3-1788845163
PASS Elasticsearch event search: hits=1 event_id=stage3-1788845163
PASS Elasticsearch ILM policy: knowtrace-logs-7d
PASS Kibana data view: knowtrace-logs-*
RESULT all requested observability checks passed
```

前一轮通过事件：`stage3-1788844577`，Elasticsearch `_count` 返回 1。

运行态采样（UTC 05:17:58）：

```text
Elasticsearch  healthy  300.2 MiB / 700 MiB
Logstash       healthy  417.4 MiB / 512 MiB
Kibana         healthy  143.6 MiB / 768 MiB
主机内存       1.8 GiB total / 409 MiB available
Swap           4.0 GiB total / 2.0 GiB used
索引           knowtrace-logs-2026.09.08 green, 1900 docs, 1.8 MiB
```

端口均只绑定回环：

```text
127.0.0.1:9200 -> Elasticsearch
127.0.0.1:5000 -> Logstash verification input
127.0.0.1:5601 -> Kibana
```

## 真实问题与修复

1. Docker Engine 29.1.3 对仅连接 internal bridge 的容器没有实现已请求的 host port；新增 `logging-management` 普通 bridge，同时保持端口只绑定 127.0.0.1。
2. Kibana 384 MiB Node 堆在启动时退出 134，日志为 `JavaScript heap out of memory`；调整为 512 MiB 堆/768 MiB 容器上限。
3. 验证事件使用字符串 `source`，与 ECS 对象映射冲突并进入 DLQ；改为 `verification_source`。
4. 默认 10 秒停止导致 Elasticsearch/Logstash 退出 137；改用 60 秒宽限并复测。

## 最终停止证据

```text
STOP_TEST_STARTED_UTC=2026-09-08T05:26:20Z
STOP_TEST_FINISHED_UTC=2026-09-08T05:27:22Z
Elasticsearch exit=143 oom=false
Logstash       exit=0   oom=false
Kibana         exit=0   oom=false
127.0.0.1:9200/5000/5601 listeners=none
```

停止后主机可用内存恢复到约 974 MiB，Swap 可用约 3.2 GiB；核心监控验收仍全通过。Elasticsearch 的 143 表示收到 SIGTERM，不是 OOM。

## 存储与边界

- 卷：`knowtrace_elasticsearch_data`、`knowtrace_logstash_data`、`knowtrace_kibana_data`。
- 本轮没有删除卷；错误事件保留在 Logstash DLQ，作为映射冲突证据。
- 拉取 ELK 镜像后根分区约 22 GiB 已用、32 GiB 可用（41%）。
- 此结果证明一次端到端链路，不证明长期日志容量、检索性能或生产保留合规。
