# 2026-09-08 公网 HTTPS 与最终状态

## Windows 公网直连

使用 `--noproxy '*'` 绕过本机代理链，但没有关闭 TLS 校验：

```text
ready_https=200 remote_ip=45.64.74.99 ssl_verify=0
metrics_public=404 remote_ip=45.64.74.99 ssl_verify=0
home_https=307 redirect=https://knowtrace.duckdns.org/login ssl_verify=0
```

DNS A 记录：`knowtrace.duckdns.org -> 45.64.74.99`；443 TCP 连接成功。

## VPS 视角

```text
ready=200 remote=45.64.74.99 verify=0
metrics=404 remote=45.64.74.99 verify=0
```

证书：

```text
subject=CN = knowtrace.duckdns.org
issuer=C = US, O = Let's Encrypt, CN = YE2
notBefore=Sep  6 13:17:39 2026 GMT
notAfter=Dec  5 13:17:38 2026 GMT
SAN=DNS:knowtrace.duckdns.org
```

## 最终运行态

- 核心业务与 Prometheus/Grafana/Alertmanager/Exporters 正常运行。
- ELK 三个容器停止，卷保留。
- 公网监听为 80、443、22345；阶段三没有开放监控管理端口。
- UFW 旧的 22/OpenSSH IPv4、IPv6 allow 规则已在备份后删除；外部 22345 可连接、22 不可连接，新的密钥 SSH 会话和 HTTPS ready 均通过。
- 3001、9090、9093 仅回环监听。
- 最终核心验收 12/12 targets UP，active alerts=0。
- 当前 commit `31f49641d310c693d829ce8ca6b7a24078a196ed` 与远端同名分支一致；该提交新增 163 SMTP 安全配置助手与 465 TLS 防误配校验，尚未合并 `main`。

## 本机代理误判说明

首次 Windows curl 经本机代理链出现 Schannel handshake failure；同一时间 VPS 直连验证成功。加入 `--noproxy '*'` 后 Windows 也成功，因此没有修改服务器证书、关闭 TLS 校验或重签证书。此现象记录在 `ISSUE-S3-008`。
