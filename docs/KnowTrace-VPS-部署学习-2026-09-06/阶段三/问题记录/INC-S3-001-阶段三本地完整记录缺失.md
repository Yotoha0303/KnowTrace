# INC-S3-001：阶段三本地完整记录缺失

## 现象

用户在本地交付目录只看到阶段三摘要，没有看到 ELK Compose、启动脚本和完整
Runbook。摘要原本指向
`C:\Users\Yotoha\Desktop\KnowTrace-VPS-部署学习-2026-09-06\阶段三`，但
2026-09-09 只读检查没有发现该目录。

## 影响

- ELK 在 VPS 上仍可运行，不影响服务器已有部署；
- 用户无法直接从本地交付目录学习完整实现；
- 原摘要对本地材料位置的描述不准确。

## 核验结果

- VPS 项目目录：`/opt/knowtrace`；
- ELK 脚本：`/opt/knowtrace/scripts/linux/elk.sh`；
- Compose overlay：`/opt/knowtrace/compose.observability.yaml`；
- 完整 Runbook：`/opt/knowtrace/docs/16-stage3-observability.md`；
- 当前远端 Git 分支：`codex/stage3-observability`；
- 2026-09-09 VPS HEAD 与 GitHub 远端分支均为
  `31f49641d310c693d829ce8ca6b7a24078a196ed`；
- `compose.production.yaml` 为 VPS 专用未跟踪文件；敏感 `.env` 不进入 Git。

## 处理

- 修正阶段三摘要中的错误路径；
- 新增 `02-ELK按需启动与访问.md`，记录启动、验收、SSH 隧道、停止和回滚边界；
- 保留远端 Git 分支作为完整代码来源，不把秘密配置复制进本地记录。

## 后续改进

如果需要完整离线学习副本，应从 `origin/codex/stage3-observability` 建立独立
Git worktree，并验证文件清单和 commit；不得直接复制 VPS 的 `.env`。
