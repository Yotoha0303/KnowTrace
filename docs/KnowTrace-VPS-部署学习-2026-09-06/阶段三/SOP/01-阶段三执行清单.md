# 阶段三执行清单

## A. 变更前门禁

- [x] 确认 SSH 别名使用 `45.64.74.99:22345`、root、指定私钥，禁止密码回退。
- [x] 确认公网 ready、认证 ready 和现有容器健康。
- [x] 创建部署前全量备份并执行隔离恢复验证。
- [x] 部署前备份 Nginx 配置并执行 `nginx -t`。
- [x] 记录生产叠加文件 SHA-256，避免分支切换覆盖未跟踪配置。
- [x] 检查 1.8 GiB 内存、4 GiB Swap 和磁盘余量。

部署前备份证据：

```text
/var/backups/knowtrace/knowtrace-20260908T025626Z-b2b82b6d.tar.gz
SHA-256: bada1470eb8fc4c379c1a71958f4a8dcf433f16a53f7c1618ec19d4c730e2807
RESTORE_VERIFY=PASS
```

## B. 部署核心监控

```bash
ssh knowtrace-vps
cd /opt/knowtrace
git status --short --branch
git pull --ff-only origin codex/stage3-observability
scripts/linux/deploy-observability.sh
python3 scripts/linux/verify-observability.py --core
```

- [x] Prometheus、Alertmanager、Grafana、Node Exporter、Blackbox Exporter 启动。
- [x] 应用镜像包含 `/api/metrics`。
- [x] Nginx 对公网 `/api/metrics` 固定返回 404。
- [x] 12/12 Prometheus targets UP。
- [x] 应用、数据库、认证、公网探测、备份新鲜度 PromQL 有样本。
- [x] Grafana 数据源和 dashboard 通过 API 验证。
- [x] Prometheus 发现 active Alertmanager。

## C. 安全检查

- [x] 3001、9090、9093 只绑定 `127.0.0.1`。
- [x] 9200、5000、5601 仅在 ELK 按需运行期间绑定 `127.0.0.1`。
- [x] 没有为监控或 ELK 新增 UFW 公网放行。
- [x] 备份 UFW 后删除未监听的 22/OpenSSH 放行；新连接验证 22345 可用、22 不可用。
- [x] `.env.observability` 权限 0600；派生 token/Alertmanager 配置权限 0400。
- [x] 本地记录没有保存密码、Token、Cookie 或 `.env` 内容。

## D. 告警故障演练

```bash
cd /opt/knowtrace
scripts/linux/observability-drill.sh blackbox
```

- [x] 演练前核心验收通过。
- [x] 仅停止 Blackbox Exporter，不停止应用或数据库。
- [x] `KnowTraceMetricsTargetDown` 进入 firing。
- [x] Alertmanager 收到告警。
- [x] Blackbox Exporter 自动恢复。
- [x] 12/12 targets 恢复，告警清除。
- [x] 证据日志以 0600 保存。

## E. ELK 按需验证

```bash
cd /opt/knowtrace
scripts/linux/elk.sh up
```

验收内容：

- [x] Elasticsearch、Logstash、Kibana healthy。
- [x] 9200、5000、5601 只绑定回环地址。
- [x] 验证事件经 Logstash 写入 Elasticsearch，查询命中 1。
- [x] `knowtrace-logs-7d` ILM 存在。
- [x] Kibana `knowtrace-logs-*` data view 存在。
- [x] 记录运行时内存、Swap、磁盘和容器状态。

完成后必须停止且保留数据卷：

```bash
scripts/linux/elk.sh stop
```

- [x] Kibana/Logstash 正常退出，Elasticsearch 收到 SIGTERM。
- [x] 9200、5000、5601 停止监听。
- [x] 未运行 `docker compose down --volumes`。
- [x] 停止后核心监控仍 12/12 targets UP。

## F. 邮件告警

- [x] SMTP 提供商选择 163；VPS 到 `smtp.163.com:465` 的 TLS 握手和证书校验通过。
- [ ] 用户确认完整发件地址、收件地址。
- [ ] 作废聊天中出现过的授权码，再通过 VPS 隐藏交互提示写入新授权码。
- [ ] 以 `ALERT_SMTP_REQUIRE_TLS=false` 跳过 Alertmanager 0.28.1 在 465 上的重复 STARTTLS。
- [ ] `amtool check-config` 通过。
- [ ] 重建 Alertmanager 并运行 `scripts/linux/test-email-alert.sh`。
- [ ] Alertmanager 日志无发送错误。
- [ ] 收件箱实际收到测试告警。

在最后两项完成前，阶段三状态必须写为“核心可观测性完成，外部邮件待验收”。

## G. 最终验证与回滚

```bash
python3 scripts/linux/verify-observability.py --core
docker ps
ss -ltn
free -h
df -h /
```

- [x] 当前 commit 与远端分支一致。
- [x] 公网 ready=200，首页跳转 `/login`，metrics=404，证书校验通过。
- [x] ELK 已停止，核心监控常驻。
- [x] Nginx 回滚备份：`/root/knowtrace-ops/backups/20260908T030449Z-stage3-nginx`。
- [x] 部署前数据备份仍存在且 SHA-256 未变。
- [x] UFW 回滚备份：`/root/knowtrace-ops/backups/20260908T054929Z-pre-remove-ufw-22`。

发生问题时先停止 ELK、保存日志和资源证据，再按项目 `docs/16-stage3-observability.md` 回滚；不要删除监控/日志卷，不要关闭 UFW 或 TLS 校验来规避问题。
