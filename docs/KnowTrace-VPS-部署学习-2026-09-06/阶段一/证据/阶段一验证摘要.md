# 阶段一验证摘要（2026-09-06）

## 已验证

| 检查项 | 结果 |
|---|---|
| SSH 密钥登录 | 独立批处理连接成功；密码登录已禁用 |
| Docker | daemon active；`hello-world` 成功 |
| Compose | 配置合并校验成功；7 个服务均设置日志轮转 |
| 长期容器 | app、auth、PostgreSQL、MySQL、Redis 均 healthy，重启次数 0 |
| 一次性任务 | auth-migrate、auth-bootstrap 均 exit 0 |
| 应用健康 | 应用直连和 Nginx 反代的 live/ready 均 HTTP 200 |
| 认证 | 初始管理员登录 HTTP 200；响应体被丢弃 |
| Nginx | 配置测试通过，仅监听 127.0.0.1:8080；错误日志 0 行 |
| Caddy | 配置测试通过，服务 active，监听 80/443；HTTP 返回 308 |
| 日志 | Docker、Nginx、Caddy 日志路径存在并可读取 |
| DNS | Cloudflare DNS 与 VPS 均解析到 45.64.74.99 |
| TLS | Let’s Encrypt 证书签发成功，域名匹配；有效期至 2026-12-05 UTC |
| 公网页面 | HTTPS 登录页 HTTP/2 200；live/ready HTTP 200 |
| 安全头 | HSTS、X-Content-Type-Options、Referrer-Policy 已返回 |
| 主机资源 | 部署后根分区使用约 13 GiB/57 GiB；可用内存约 854 MiB，Swap 使用约 230 MiB |

## 尚未验证

- 新内核尚未通过计划重启加载；重启后的 Compose 自动恢复尚未验证。
- 当前 Windows 终端使用直连 `curl` 时访问 80/443 失败，但应用内浏览器可打开登录页，Let’s Encrypt 多个外部验证节点也成功访问；需后续单独排查本机代理/网络路径。

阶段一已经上线；以上残留项不等同于备份、恢复或生产高可用证明。
