# 服务器基线（2026-09-06）

## 已确认事实

| 项目 | 结果 |
|---|---|
| VPS | `45.64.74.99` |
| 系统 | Ubuntu 24.04.4 LTS |
| 内核（运行中） | `6.8.0-35-generic` |
| 待启用内核 | `6.8.0-139-generic`，需要计划重启 |
| 计算资源 | 2 vCPU、约 1.8 GiB RAM、4 GiB Swap |
| 根分区 | 57 GiB，总可用约 47 GiB（盘点时） |
| SSH | 22/tcp，OpenSSH 9.6p1 服务端 |
| Nginx | 1.24.0，配置测试通过 |
| Docker | 29.1.3，Ubuntu 软件源 |
| Compose | 2.40.3，Ubuntu 软件源 |
| Caddy | 2.11.4，Caddy 官方稳定软件源 |
| UFW | 已从 inactive 改为 active；默认拒绝入站，允许 22/80/443 |
| 域名 | `knowtrace.duckdns.org`；已更新到 `45.64.74.99` |
| VPS 仓库版本 | `main`，提交 `42c1f1e692653c558af9631a5ba064b2b828d3b3` |

## 已知容量风险

- 1.8 GiB 内存同时构建 Next.js、Go、PostgreSQL、MySQL、Redis，可能触发 Swap，首次构建会慢。
- 单 VPS、单磁盘不是高可用；阶段三备份必须离开本机并做恢复验证。
- 系统提示需要重启加载新内核；阶段一完成后应安排一次可验证重启。
- 本机权威工作树仍为 `1517afc`，VPS 从 GitHub 获取到更新的 `42c1f1e`；两者版本不同，后续发布前要先决定以哪一个提交为准。
