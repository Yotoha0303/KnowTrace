# 阶段一操作记录

日期：2026-09-06  
目标：将 KnowTrace 通过 `https://knowtrace.duckdns.org` 对外提供服务，并保留日志与回滚证据。

## 1. 只读盘点

- [x] 确认权威仓库：`C:\Users\Yotoha\Desktop\KnowTrace`
- [x] 确认 Git 分支：`main`，工作树干净，HEAD `1517afc`
- [x] 确认 Compose 服务：app、postgres、auth、auth-mysql、auth-redis、两个一次性任务
- [x] 确认 VPS 资源、端口、服务、UFW 与 DNS

## 2. 安全基线

- [x] 备份 UFW、Nginx 和 SSH 配置
- [x] UFW 放行 22/80/443 后启用
- [x] 创建 VPS 专用 Ed25519 密钥并安装公钥
- [x] 第二连接验证成功后关闭密码登录

## 3. Docker 与应用

- [x] 安装 Docker Engine 与 Compose 插件
- [x] `hello-world` 拉取并运行成功
- [x] 从 GitHub 克隆 KnowTrace 到 `/opt/knowtrace`
- [x] 创建权限为 `600` 的生产 `.env`
- [x] 创建 MySQL、Redis 外部卷
- [x] 校验合并后的 Compose 配置
- [x] 构建并启动完整栈
- [x] 验证所有长期服务健康、一次性任务成功退出
- [x] 验证应用与认证 live/ready 四个端点均为 HTTP 200
- [x] 使用服务器本地请求验证初始管理员登录返回 HTTP 200（响应体丢弃，凭据未写入记录）

## 4. 入口与 HTTPS

- [x] DuckDNS 更新到 `45.64.74.99`
- [x] Nginx 改为仅监听 `127.0.0.1:8080`，配置测试通过
- [x] 安装并配置 Caddy 监听 80/443，配置测试通过
- [x] 自动签发 Let’s Encrypt 证书
- [x] 公网 HTTPS、跳转、安全头和登录页面验证

## 5. 日志

- [x] Docker JSON 日志限制为单文件 10 MiB、保留 5 个文件
- [x] Nginx 独立 access/error 日志
- [x] Caddy JSON access 日志，滚动保留
- [x] 记录常用查看命令

## 验收标准

```text
DNS A 记录 = 45.64.74.99
HTTP 自动跳转 HTTPS
HTTPS 证书域名匹配且有效
登录页可加载
管理员登录成功
/api/health/live 返回 2xx
/api/health/ready 返回 2xx
数据库和认证端口未暴露到公网
容器日志、Nginx 日志、Caddy 日志均可读取
```

## 已取得的验证证据

- app、auth、PostgreSQL、MySQL、Redis：运行且健康，重启次数均为 0。
- auth-migrate、auth-bootstrap：退出码均为 0；数据库迁移到版本 6，管理员初始化完成。
- 应用直连与 Nginx 反代后的 `/api/health/live`、`/api/health/ready`：HTTP 200。
- Docker 7 个服务：`json-file`，`max-size=10m`，`max-file=5`。
- Caddy：服务 active，监听 `*:80` 和 `*:443`；HTTP 本机测试返回 308 到 HTTPS。
- Let’s Encrypt：HTTP-01 从多个外部节点验证成功，证书签发成功。
- 公网访问：根路径最终到 `https://knowtrace.duckdns.org/login` 并返回 HTTP 200；live/ready 均为 200。
- HTTPS：证书主题匹配 `knowtrace.duckdns.org`；HTTP 308 跳转，HTTP/2 200，HSTS、nosniff 和 Referrer-Policy 已返回。
