# 文件来源、完整性与敏感信息边界

## 快照范围

2026-09-09 从 VPS `45.64.74.99:22345` 只读复制了与阶段一至三直接相关的非敏感文件，包括：

- `/opt/knowtrace` 下的 Compose、部署配置、运维脚本和阶段 Runbook；
- `/etc/caddy`、`/etc/nginx`、`/etc/ssh/sshd_config.d` 的相关已安装配置；
- `/etc/systemd/system/knowtrace-backup.*`；
- Caddy APT source list 和 keyring；
- 一份脱敏的可观测性故障演练日志。

`服务器文件快照-SHA256.csv` 共记录 40 份本地副本，对应 39 个唯一 VPS 源文件；重复项是同一个最终 Nginx 配置按阶段一和阶段三各保留一份。全部 `RemoteMatch=True`。

## 三种材料不能混淆

1. `文档/` 与 `配置样例/`：历史过程记录，反映当时执行阶段；
2. `服务器文件快照/`：2026-09-09 VPS 最终状态的逐字节副本；
3. Git 分支 `codex/stage3-observability`：被版本控制的项目文件，不包含服务器秘密和专用 overlay。

VPS 仓库 HEAD 与远端分支核验为：

```text
31f49641d310c693d829ce8ca6b7a24078a196ed
```

## 主动排除的内容

以下内容没有复制到本目录：

- `C:\Users\Yotoha\.ssh\knowtrace_vps_ed25519` 私钥；
- `/root/.ssh` 与 `authorized_keys`；
- `/opt/knowtrace/.env`；
- `/opt/knowtrace/.env.observability`；
- 渲染后的 Alertmanager 配置；
- metrics bearer token、Grafana真实密码、SMTP授权码；
- `/var/backups/knowtrace` 归档、SQL dump、Redis RDB；
- 上传附件、用户记录、Cookie 和浏览器会话；
- Elasticsearch 数据卷和业务数据库 volume。

示例 `.env.example` 包含公开的开发默认值，它们不是当前服务器凭据，也不得原样用于生产。

## 完整性检查

归档完成后执行了本地敏感信息扫描，结果为：

- 私钥 PEM/OpenSSH 标记：0；
- 常见云/API Token 格式：0；
- 非示例 `.env`：0；
- SQL dump、Redis RDB、age 密文或 tar.gz 备份：0；
- 非 `example.com` 邮箱地址：0。

该扫描只能发现预设模式，不代替人工审查；它与明确白名单复制、真实 `.env` 排除和 SHA-256 对比共同组成归档验收。

在 PowerShell 中检查服务器副本：

```powershell
$Root = 'C:\Users\Yotoha\Desktop\KnowTrace-VPS-部署学习-2026-09-06'
Import-Csv "$Root\清单\服务器文件快照-SHA256.csv" |
  Group-Object RemoteMatch
```

预期只有 `True` 一组，共 40 项。

`VPS源文件权限与大小.tsv` 保留原 Linux mode、owner 和文件大小。Windows 文件系统不会保留 Unix 可执行位；恢复到 Linux 时应参考该清单重新执行 `chmod`，而不是假设 `.sh` 自动可执行。

整个学习包另有 `SHA256SUMS.txt`。它用于发现本地文件后续变化，不代表文件内容安全或业务验收通过。

## 更新规则

服务器配置或脚本变更后，应重新执行：

1. 只复制明确白名单中的非敏感文件；
2. 扫描私钥标记、非示例 `.env`、备份和真实邮箱地址；
3. 比对 VPS 与本地 SHA-256；
4. 更新当前状态快照与问题记录；
5. 重新生成 `SHA256SUMS.txt`。
