# 2026-09-07 SSH 与 HTTPS 复测证据

> 说明：本文件前半部分保留当时按默认 22 端口采集的原始现象；后续服务器证据已确认真实 SSH 端口为 22345，见文末“恢复与最终结论”。

- 采集时间：2026-09-07T22:09:17+08:00。
- 客户端：Windows PowerShell。
- VPS：`45.64.74.99`。
- 域名：`knowtrace.duckdns.org`。

## DNS

`Resolve-DnsName` 返回 A 记录 `45.64.74.99`。

## HTTPS

通过当前 Windows 代理访问 ready：

```text
status=200 remote=127.0.0.1 tls=0 total=1.541144
exit=0
```

其中 `remote=127.0.0.1` 是本机代理地址，不能单独证明 VPS 直连。

绕过代理直接访问：

```text
status=200 remote=45.64.74.99 tls=0 total=0.319440
exit=0
```

结论：本次采集时域名、TLS 校验和公网 ready 链路可用。

## SSH

使用现有私钥、批处理模式、10 秒连接超时进行一次连接：

```text
banner exchange: Connection to UNKNOWN port -1: Connection refused
exit=255
```

2026-09-07T22:26:54+08:00 与 22:55:31+08:00 两次低频复测仍得到相同的 banner exchange 拒绝，退出码均为 255。

2026-09-07T23:06:07+08:00 第四次低频复测：

```text
DNS A: knowtrace.duckdns.org -> 45.64.74.99
TCP 22: actively refused
TCP 80: open
TCP 443: open
HTTPS ready: status=200 remote=45.64.74.99 tls=0 time=0.211716s
SSH: banner exchange: Connection to UNKNOWN port -1: Connection refused
exit=255
```

同一轮中的 HTTP 根路径请求在建立 80/TCP 后被重置，状态码未取得；这不影响已独立验证成功的 HTTPS ready 结论，但应在恢复 SSH 后检查 Caddy 的 80 端口重定向和访问日志。

连接未进入用户认证阶段，因此该输出不能证明私钥错误。它只能证明本次 SSH 握手在 banner/key exchange 前被远端或链路拒绝。

## 当时的证据边界

- HTTPS 200 不证明 SSH 正常。
- 应用可用不等于远程管理能力可用。
- 尚无服务器端 `journalctl`、监听端口或防火墙证据，根因未知。
- 云厂商管理页的自动化读取连续两次超时，已按安全边界停止 UI 控制；没有执行面板变更。

## Ready 响应详情

2026-09-07T22:27:07+08:00 绕过代理再次请求，返回：

```text
HTTP/1.1 200 OK
Strict-Transport-Security: max-age=31536000; includeSubDomains
Via: 1.1 Caddy
{"status":"ok","database":"connected"}
```

这进一步证明当时的 Caddy → Nginx → app → PostgreSQL ready 链路可用，但仍不能覆盖认证服务和 SSH 管理面。

## 恢复与最终结论

用户随后补充 SSH 端口 `22345`。使用已有私钥连接成功，服务器侧复核结果为：

```text
sshd config: Port 22345
listen: 0.0.0.0:22345 and [::]:22345
ssh.service: active/enabled
sshd -t: PASS
authentication: public key only
```

Windows 已写入 `Host knowtrace-vps` 别名并用 `ssh -G knowtrace-vps` 验证：主机 `45.64.74.99`、用户 `root`、端口 `22345`、指定私钥、禁止密码回退、严格主机密钥检查。别名随后完成真实登录。

最终根因是客户端误用默认 22 端口；没有证据表明 sshd 曾停止或私钥损坏。22/TCP 的主动拒绝不能再解释为 VPS SSH 故障。

2026-09-08 09:49（Asia/Shanghai）受控重启后再次验证：

- sshd 仍在 22345 监听并可登录；
- Cloudflare DNS 直查为 `45.64.74.99`；
- HTTPS ready 绕过本机代理直连 `45.64.74.99` 返回 200，TLS 校验结果 0；
- app、auth、Nginx 和公网 ready 均通过。
