# 2026-09-07 本地脚本验证

- 验证环境：Windows 10、PowerShell 7.6.5、Python 3.14.2、Git for Windows Bash。
- 项目分支：`codex/stage2-backup-recovery`。
- 版本提交：`0064f879c8eb6227d3cca7a27cd33a09938f8da1`（初版）与 `17251a07f0fed985dd9f85fc19d24bd725d1aba4`（恢复演练加固）。
- 远端状态：分支已推送到 `origin/codex/stage2-backup-recovery`，未合并 `main`、未部署 VPS。
- 证据级别：本地静态/烟雾测试，不是 VPS 运行结果。

## Bash

```text
backup-all.sh: bash -n exit=0
verify-restore.sh: bash -n exit=0
```

缺少备份路径时，恢复脚本以退出码 1 拒绝继续。Windows Git Bash 没有 `flock`，因此全量备份脚本只能完成语法检查；Ubuntu VPS 的真实 Compose/数据库行为仍待 SSH 恢复后验证。

## Python

```text
python -m py_compile: exit=0
requests=20
concurrency=4
HTTP 200=20
failed_requests=0
script exit=0
```

烟雾测试目标是本机临时 HTTP 服务，不是 KnowTrace VPS。另行测试 `--concurrency 51` 时，脚本按设计拒绝执行并返回退出码 2，证明并发硬上限生效。

## Git 文本检查

```text
git diff --check: exit=0
```

该结果只证明当前补丁没有 Git 可检测的空白错误。

## 项目质量门禁

```text
pnpm typecheck: exit=0
pnpm lint: exit=0，0 error，4 个既有 warning
pnpm test: exit=0，36 个测试文件、141 项测试通过
pnpm build: exit=0，Next.js 生产构建成功
go test ./...: exit=0
```

4 个 warning 位于既有 `data-transfer` 文件中的未使用变量，与本次阶段二 Bash/Python/文档变更无关；它们未被本次迭代顺带修改。
