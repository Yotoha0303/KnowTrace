# 2026-09-08 版本发布与受控重启证据

## 版本迭代

- 部署前 commit：`42c1f1e692653c558af9631a5ba064b2b828d3b3`。
- 当前 commit：`fcadab5edb63ed940a9b3d891842bbfd39e5a9d7`。
- 分支：`codex/stage2-backup-recovery`，已推送 `origin`，尚未合并 `main`。
- 关键提交：`0064f87` 备份/恢复/压测工具；`17251a0` 恢复加固；`87da544` 定时和保留；`f4c5580` 安装文档修正；`fcadab5` TLS 信任显式化。
- VPS 特有 `compose.production.yaml` 是未跟踪部署叠加文件，部署前后 SHA-256 均为 `85ec35b01bd0da1b5123577080a960be42ab5db1430c5386ceccc56f76549586`，没有被 Git 覆盖。

发布前已有真实归档和隔离恢复通过；项目门禁包括 TypeScript 类型检查、lint、141 项测试、构建和 Go 测试。lint 有 4 条既有且与本次无关的 warning，没有 error。

## 受控重启原因与预检

系统升级后 `/var/run/reboot-required` 存在，正在运行内核为 `6.8.0-35-generic`，已安装新内核 `6.8.0-139-generic`。重启前完成：

- 仅有 root 的旧 tty1 会话，没有其他活动 SSH 用户；
- `sshd -t`、`nginx -t`、Caddy 配置校验通过；
- ssh、docker、nginx、caddy 和备份 timer 均 enabled；
- 5 个 Compose 容器 healthy，公网 ready 通过；
- 已有两份归档，其中定时归档已经隔离恢复通过。

## 恢复时间线

| 时间（Asia/Shanghai） | SSH | HTTPS ready |
| --- | --- | --- |
| 09:44:24 | 不可达 | 000 |
| 09:44:35 | 已恢复 | 502 |
| 09:44:42 | 已恢复 | 502 |
| 09:44:47 | 已恢复 | 200 |

从发出重启到公网 ready 约 23 秒。短暂 502 表明边缘代理先于上游容器恢复，是重启窗口内的预期启动次序；没有把它写成零停机。

## 重启后终验

2026-09-08 09:49（Asia/Shanghai）：

- 内核 `6.8.0-139-generic`，`reboot-required=no`；
- ssh、docker、nginx、caddy、`knowtrace-backup.timer` 均 active/enabled；
- sshd 在 IPv4/IPv6 的 22345 监听，Windows 别名真实登录成功；
- app、auth、Nginx 和公网 ready 全部通过；
- 5 个容器均 running/healthy；
- 当前版本为 `fcadab5...`，生产叠加哈希未变；
- 定时器保留，下一次触发为 2026-09-09 03:20 左右（Asia/Shanghai）。

日志：`/var/log/knowtrace-maintenance-reboot-20260908.log`，以及各 systemd/Caddy/Nginx/容器日志。

## 非 KnowTrace 残留

- `setupfirst.service` 的无效残留已备份、禁用并移出加载路径；重启后为 not-found，见 `INC-S2-002`。
- `repass.service` 是厂商密码重置辅助服务，仍为唯一 failed unit；因它可能用于控制台救援，未擅自禁用，见 `INC-S2-003`。
- 启动日志还有虚拟软驱 `fd0`、无介质 `sr0` 和虚拟 SMBus 提示；目前没有证据表明影响 KnowTrace。

## 回滚方法

若确认新版本引入应用回归：先保存当前日志和新备份，再将 `/opt/knowtrace` 切回已记录的 `42c1f1e...`，保留现有 `.env` 与 `compose.production.yaml`，重新构建/启动后验证 app、auth、Nginx、公网 ready 和业务读取。不要删除 volume，也不要使用 `docker compose down --volumes`。
