# 2026-09-08 阶段二远端执行结果

## 已验证

- SSH：`45.64.74.99:22345`，公钥登录通过；22 端口拒绝是错误连接信息，不是 sshd 宕机。
- 网站：`knowtrace.duckdns.org` 解析到目标 VPS；公网 HTTPS ready 直连返回 200，TLS 校验通过。
- 版本：VPS 当前 commit `fcadab5edb63ed940a9b3d891842bbfd39e5a9d7`，生产叠加文件哈希未变；回滚点 `42c1f1e692653c558af9631a5ba064b2b828d3b3`。
- 备份：手工和 systemd 定时归档各 1 份，均为 27091 B、0600/root；每天 03:20（Asia/Shanghai）运行，14 天/至少 7 份保留策略。
- 恢复：两份归档均 `RESTORE_VERIFY=PASS`；PostgreSQL 23 表、MySQL 7 表、Redis 0 key，上传目录只有 1 个 `.gitkeep` 占位文件。
- 异地：age 密文已复制到桌面，密文哈希及解密流哈希匹配，没有在 Windows 留下明文归档。
- 压测：内部 Nginx 200/并发 5 为 177.349 RPS、P95 47.065 ms；公网 HTTPS 200/并发 5 为 11.744 RPS、P95 1085.687 ms；两组均零失败且无新增应用错误。
- 重启：内核从 `6.8.0-35` 切换为 `6.8.0-139`，公网 ready 约 23 秒恢复；重启后 5 个容器 healthy，备份 timer 仍 active/enabled。

## 未验证与风险

- 线上 `evidence_attachments=0`，所以没有真实附件可验证。
- 当前管理员密码不在服务器初始化配置中；没有重置凭据。业务层登录、历史记录和附件读取待用户准备测试数据。
- 健康端点基线不是业务容量、长稳或真实用户结果。
- age 暂时复用 SSH 密钥，缺少专用备份密钥和第二存储位置。
- 厂商辅助 `repass.service` 仍失败；因为可能影响控制台密码救援，未擅自禁用。
- GitHub 报告默认分支存在 1 个 moderate dependency alert，本阶段未扩大范围处理。

## 下一步

用户在网站中准备一条非敏感测试记录和一个真实附件后，只需回复“已准备测试数据”，不要发送密码。随后补做独立恢复环境中的业务级读取，阶段二才能完整关闭。
