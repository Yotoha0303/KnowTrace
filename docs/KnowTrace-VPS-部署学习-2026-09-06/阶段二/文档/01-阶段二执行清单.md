# 阶段二执行清单

## 管理通道

- [x] 确认 SSH 真实端口 22345，而不是默认 22。
- [x] 保存 ssh 服务、监听端口、日志、防火墙、磁盘和内存证据。
- [x] 明确根因是客户端端口信息错误；服务器 sshd 未宕机。
- [x] Windows 现有密钥独立登录成功并建立安全别名。

## 数据保护

- [x] 说明短暂维护窗口。
- [x] 服务器执行 `backup-all.sh`，生成全量归档与 SHA-256。
- [x] 手工和定时归档均由 `verify-restore.sh` 输出 `RESTORE_VERIFY=PASS`。
- [ ] 已知业务记录和附件读取通过。
- [x] 加密副本离开 VPS，并完成解密流哈希校验。
- [ ] 将当前复用的 SSH 接收者迁移成专用备份密钥。

## 压测

- [x] 压测前保存 ready、资源、容器和日志基线。
- [x] 内部和公网首档只读健康端点：各 200 请求、并发 5、零失败。
- [x] 保存错误率、RPS、P95、P99、资源和错误日志。
- [x] 不把健康端点基线写成业务容量或真实用户结果。

## 迭代

- [x] 独立分支和本地 commit。
- [x] 类型、Lint、测试、构建、Go 测试门禁。
- [x] 分支推送，未合并 `main`。
- [x] 本地与 VPS 隔离恢复通过，5 个发现的 Bug 完成回归。
- [x] 发布前备份与恢复验证。
- [x] VPS 部署、线上验证与明确回滚点。
- [x] 受控重启后 SSH、HTTPS、容器、timer 和版本终验通过。
- [ ] 使用当前账号完成业务登录、历史记录和真实附件读取。
