# 阶段二执行清单

## A. 恢复并固化管理通道

- [x] 确认公网应用在线，避免把错误端口误判成整机故障。
- [x] 保存 `systemctl status ssh`、`ss -ltnp`、`journalctl -u ssh`、UFW 和配置证据。
- [x] 查明事实根因：客户端误用 22，sshd 实际监听 22345。
- [x] 从 Windows 使用现有密钥在 22345 独立登录成功。
- [x] 建立 `ssh knowtrace-vps` 别名，并验证只允许公钥、禁止密码回退。
- [x] 保留 SSH 配置原值备份，路径为 `C:\Users\Yotoha\.ssh\config.pre-knowtrace-20260908`。

## B. 首次全量备份

- [x] 说明短暂维护窗口的影响。
- [x] 检查 ready、容器、磁盘、内存和当前 commit。
- [x] 将项目分支中的 `scripts/linux` 部署到 `/opt/knowtrace/scripts/linux`。
- [x] 执行 `backup-all.sh`，记录归档路径、大小和 SHA-256。
- [x] 确认维护窗口结束后 app/auth 均恢复 healthy。
- [x] 公网 HTTPS ready 通过。
- [ ] 使用当前应用账号完成登录、历史记录和真实附件读取；不能用数据库计数代替业务验收。

## C. 恢复与异地

- [x] `verify-restore.sh` 对手工和定时归档均输出 `RESTORE_VERIFY=PASS`。
- [x] 记录 PostgreSQL 23 表、MySQL 7 表、Redis 0 key、上传目录 1 个占位文件。
- [ ] 改用独立备份密钥；当前密文使用已有 SSH 密钥作为 age 接收者，是已记录的隔离偏差。
- [x] 密文复制到 VPS 外的受限目录并核对 SHA-256。
- [x] 用解密流核对明文归档 SHA-256；本次没有在 Windows 落地明文。
- [ ] 独立业务恢复环境完成登录、历史记录和真实附件读取。

## D. 压测与观察

- [x] 压测前保存 `uptime`、`free`、`df`、`docker stats`、ready 和日志基线。
- [x] 执行内部和公网各 200 请求、并发 5 的只读健康端点基线。
- [x] 零错误后停止在安全基线，没有擅自扩大到写入、长稳或容量压测。
- [x] 保存请求量、并发、RPS、P95、P99、错误率、CA 和代理条件。
- [x] 明确标注健康端点结果不等于业务容量或真实用户结果。

## E. Bug、故障与迭代

- [x] Bug、真实故障和操作问题分别建档。
- [x] 修复前保存复现证据，修复后保存回归证据。
- [x] 发布前备份并验证；记录旧/new commit 与生产叠加配置哈希。
- [x] 部署及重启后验证 ready、容器、版本、定时器和错误日志。
- [ ] 用当前账号完成登录与业务读取验收。
- [x] 保留 `42c1f1e` 回滚点；全过程未使用 `docker compose down --volumes`。

## F. 阶段二关闭条件

- [x] 自动备份、保留策略、隔离恢复、异地密文、基线压测、故障记录和版本迭代均有 VPS 实证。
- [ ] 用户在当前线上账号中准备一条非敏感测试记录和一个真实附件。
- [ ] 在隔离恢复环境完成业务层读取并记录结果。
- [ ] 决定是否将 SSH 复用的 age 接收者迁移为专用备份密钥。

权威命令和原理见项目文档 `docs/15-stage2-vps-reliability.md`。
